<header class="absolute w-full h-full lg:overflow-hidden {{ heroSettings.bg.color or 'bg-black' }} ">
  <div class="absolute inset-0 {{ heroSettings.bg.opacityMobile or 'opacity-100' }} {{ heroSettings.bg.opacityDesktop or 'opacity-100' }}">
    <video src="{{ heroSettings.bg.video }}" autoplay muted loop class="object-cover w-full h-full"></video>
  </div>
      {# Shared across hero components  #}
<div class="absolute flex flex-col h-full">
  {% include "components/hero-text-overlay.njk" %}
</div>
</header>